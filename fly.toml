app = "streamhub-worker"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile.worker"

[processes]
  worker = "npm run worker"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
